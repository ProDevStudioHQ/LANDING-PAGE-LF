# Content-Signal below is Cloudflare's Content Signals Policy, not a typo.
# Semrush Site Audit reports it as "Invalid robots.txt format" because its
# parser only knows the four RFC 9309 fields. Keep it anyway: RFC 9309 §2.2
# requires crawlers to ignore lines they do not recognise, so it cannot break a
# rule, and it is the only thing on this site expressing "index me, do not train
# on me" — which is the entire point of the allow/deny split further down. The
# audit error is a false positive; do not "fix" it by deleting this line.
User-agent: *
Content-Signal: search=yes,ai-train=no
Allow: /
Allow: /api/public/
Disallow: /api/
Disallow: /admin/

# AI SEARCH / RETRIEVAL — allowed (citations in ChatGPT, Claude, Perplexity,
# Google AI Overviews). Training still refused via Content-Signal above.
User-agent: GPTBot
Allow: /
User-agent: OAI-SearchBot
Allow: /
User-agent: ClaudeBot
Allow: /
User-agent: PerplexityBot
Allow: /
User-agent: Google-Extended
Allow: /

# TRAINING-ONLY / SCRAPER BOTS — keep blocked
User-agent: CCBot
Disallow: /
User-agent: Bytespider
Disallow: /
User-agent: Amazonbot
Disallow: /
User-agent: meta-externalagent
Disallow: /
User-agent: Applebot-Extended
Disallow: /

# Site rules (path allow/disallow live in the single User-agent: * group above —
# a second * group would be ignored by crawlers that honor only the first match).

Sitemap: https://digitalstudiolf.online/sitemap.xml
